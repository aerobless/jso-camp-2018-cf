import { expect } from 'chai';

import { KickerboxManager } from './kickerboxManager';

describe('Entrypoint for Kickerbox REST endpoint', () => {
  it('returns some text', () => {
    //expect(kickerboxManager()).to.eql('Hoi du! Ich bin en REST Endpoint ^__^ ... oder au nöd 42');
  });
});
